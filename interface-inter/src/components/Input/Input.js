import { InputContainer } from "./styles";

export const Input = ({ children }) => {
    return (
        <>
            <InputContainer>
                { children }
            </InputContainer>
        </>
    )
}